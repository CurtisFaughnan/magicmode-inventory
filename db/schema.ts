import { integer, real, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const items=sqliteTable("items",{id:integer("id").primaryKey({autoIncrement:true}),partNumber:text("part_number").notNull(),name:text("name").notNull(),description:text("description").notNull(),qtyNeeded:real("qty_needed").notNull(),qtyOnHand:real("qty_on_hand").notNull(),unitCost:real("unit_cost"),unit:text("unit").notNull(),supplier:text("supplier").notNull(),leadTime:text("lead_time").notNull(),bomLevel:integer("bom_level"),notes:text("notes").notNull()});
export const transactions=sqliteTable("transactions",{id:integer("id").primaryKey({autoIncrement:true}),itemId:integer("item_id").notNull(),kind:text("kind").notNull(),quantity:real("quantity").notNull(),reference:text("reference").notNull(),note:text("note").notNull(),createdAt:text("created_at").notNull()});
